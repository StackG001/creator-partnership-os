# outreach

Personalised outreach drafted from the audit, never from a template.

Every message stays `DRAFT` until a human approves it — sending is a separate, deliberate step. Artifacts land in `outputs/<handle>/outreach/`.
