# brand

Captures how the creator sounds and looks so the generated PDF and sales page read as theirs, not ours.

Writes `Creator.brandProfile` and `outputs/<handle>/brand/brand.json` + a swatch preview.
