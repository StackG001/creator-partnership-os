# publisher

Pushes the finished funnel to Whop, records the revenue split, and syncs sales back into `Launch` so results are logged per creator.

`--dry-run` prints the exact API payload without creating anything.
